@AGENTS.md

Everything Claude-Code-specific for this repo lives in `AGENTS.md` too (it is
the canonical instructions file, kept tool-agnostic on purpose so other
agents reading this repo see the same rules). Nothing else needed here -
add new instructions to `AGENTS.md`, not this file, so there is only ever
one copy to keep in sync.
